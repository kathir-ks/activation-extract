[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    slow: marks tests that load full models (deselect with '-m "not slow"')
    gpu: marks tests requiring GPU/TPU hardware
addopts = -v --tb=short
