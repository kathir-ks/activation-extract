# Activation Extraction Pipeline - TPU Deployment Requirements
# Python 3.10+
#
# IMPORTANT: For TPU VMs, install JAX with TPU support first:
#   pip install jax[tpu] -f https://storage.googleapis.com/jax-releases/libtpu_releases.html
#
# Then install the rest:
#   pip install -r requirements.txt

# JAX ecosystem (TPU version installed separately - see above)
flax>=0.8.0

# HuggingFace ecosystem
transformers>=4.38.0
datasets>=2.17.0
huggingface-hub>=0.20.0
tokenizers>=0.15.0
safetensors>=0.4.0

# PyTorch (CPU version for weight conversion only)
torch>=2.2.0

# Prompt rendering and ARC utilities
jinja2>=3.1.0
termcolor>=2.3.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Progress and utilities
tqdm>=4.65.0

# Cloud storage (GCS support)
google-cloud-storage>=2.14.0
gcsfs>=2023.1.0

# System monitoring
psutil>=5.9.0

# Testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0

